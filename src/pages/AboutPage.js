import React from 'react';
import '../styles/css/style.css';
import '../styles/css/utils.css';


const YeghsAboutPage = () => (
  <div className="conti">
    <div className="wrapper d-flex flex-col  a-i-center">
      <h2 className="get-started1">Yeghs is a simple transaction service application build with NodeJS and MongoDb, that can allow an authenticated user to send money to another user, receive an OTP via his registered phone number to complete the transaction...</h2>
    </div>

  </div>
);

export default YeghsAboutPage;
